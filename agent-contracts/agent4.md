# Agent 4 Contract: Music Adapters

## Inputs
- `server/publicApiAdapters.js`

## Required Sections
1. Hybrid provider chain (open full-length + latest previews)
2. Normalized schema with playback fields
3. Fallback and diagnostics behavior

## Pass Criteria
- Supports 100+ items with `playbackType`, `language`, `licenseType`.
